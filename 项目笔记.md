# 铭澄互动企业官网 · 项目笔记

> 记录时间：2026-08-12
> 状态：已上线 + Git 自动部署已启用（https://mingcheng-ai.com ↔ GitHub CaelumACE/mingcheng-website）
> 维护人：浩哥（王泽浩）+ AI 助手

---

## 一、项目概况

- **主体**：铭澄互动（北京），软件服务公司
- **官网定位**：B 端企业官网，调性"简单整洁、美观又不失炫酷"
- **业务线（4 条）**：AI 数字员工 · VR/AR 虚实融合 · 无人机训练软件（占位） · 智慧交通
- **页面（4 页）**：首页 / 关于我们 / 产品服务 / 联系我们
- **适配**：PC / 平板 / 手机多端响应式
- **Slogan**：
  - 主：「铭刻创新，澄澈未来」
  - 副：「铭澄互动 —— 让智能触手可及」
- **联系人**：王泽浩 · 18610142706 · 18610142706@163.com

---

## 二、技术栈

| 项 | 选型 |
|----|------|
| 框架 | Vite + React + react-router-dom |
| 样式 | TailwindCSS v3 |
| 视觉元素 | SVG + CSS 实现（不依赖外部图库），用户提供素材用本地图片 |
| 托管 | Cloudflare Pages（真托管，免备案） |
| 域名 | mingcheng-ai.com（腾讯云注册） |

**关键路径**
- 项目根：`mingcheng-website/`
- 入口：`index.html`（含浏览器标题 `<title>铭刻创新，澄澈未来</title>`）
- 页面：`src/pages/{Home,About,Products,Contact}.jsx`
- 组件：`src/components/`（Navbar 磨砂玻璃导航 / Footer / FloatingShapes / HeroIllustration / CountUp / ScrollProgress 等）
- 素材：`public/assets/`
- 构建产物：`dist/`（部署用）

---

## 三、设计演进与关键决策

| 版本 | 方向 | 结果 |
|------|------|------|
| v1 | 浅灰蓝 + 暖金（温暖科技风） | 被否，科技感不足 |
| v2 | 深色科技 | 偏游戏感，被否 |
| **v3** | **浅蓝底 + 玻璃层叠科技风（Light Glassmorphism）** | ✅ **最终选定** |
| v3.1 | 加几何插画 / 浮动形状 | 采纳 |
| v3.2 | 背景收敛 / 字号阶梯 / 流光微交互 | 采纳 |
| v3.3 | 社会证明三件套 / 滚动进度条 | 采纳 |
| v3.4 | Slogan 定稿 | 采纳 |

**定稿风格要点**
- 浅蓝渐变底 + 半透明磨砂玻璃卡片（backdrop-blur）
- 导航栏：圆角半透明磨砂玻璃 + 汉堡菜单（移动端）
- LOGO：**纯文字"铭澄互动"**，未单独设计图形
- 微交互：流光边框、浮动几何、滚动渐入（IntersectionObserver）、数字滚动

---

## 四、页面与内容结构

### 首页（Home）
- Hero：主 Slogan + 副 Slogan + CTA
- 业务线概览（4 条）
- **标杆案例区**：党建展厅实景图（case-showroom.jpg）
- **合作伙伴墙**：6 家真实 Logo（玻璃卡片，hover 提透明度）
- CTA 区

### 关于我们（About）
- 公司简介（AI 撰写，基于公司简介 PDF）
- 能力 / 价值观板块

### 产品服务（Products）
- AI 数字员工：SVG 插画 + 4 卖点（执行系统 / 安全可控 / XR 融合交互 / 多智能体协同）
- VR/AR 虚实融合：**数字展厅实景图**（vr-ar-showroom.jpg）
- 无人机训练软件：**真实训练截图**（机舱视角，drone-training-cabin.jpg）
- 智慧交通：SVG 插画 + 四大算法（时空分层预测 / 动态图结构学习 / BiGCN / T2GAN）

### 联系我们（Contact）
- 联系人 / 电话 / 邮箱 / 微信二维码（wechat-qr.png）

---

## 五、素材清单（public/assets/）

| 文件 | 用途 | 来源 |
|------|------|------|
| case-showroom.jpg | 首页标杆案例（党建展厅） | 用户提供 |
| vr-ar-showroom.jpg | 产品页 VR/AR 主视觉 | 用户提供 |
| drone-training-cabin.jpg | 产品页无人机主视觉 | 用户提供 |
| drone-training-scenes.png | 无人机场景选择界面（**暂未使用，备用**） | 用户提供 |
| vr-ar-scenarios.jpg | VR/AR 多场景合集（**已弃用，结构太杂**） | 用户提供 |
| wechat-qr.png | 联系页微信二维码 | 用户提供 |
| logo-capgemini.png / logo-ibm.png / logo-bjut.png / logo-bmw.png / logo-unionpay.png / logo-stategrid.png | 合作伙伴 Logo | 用户提供（凯捷/IBM/北工大/BMW/银联/国网） |

**合作伙伴（6 家）**：上海银联 · 国家电网 · 北京工业大学 · BMW · IBM · 凯捷咨询

---

## 六、本地开发

```bash
# 启动开发服务器（后台）
cd mingcheng-website
NODE="C:/Users/jx/.workbuddy/binaries/node/versions/22.22.2/node.exe"
"$NODE" node_modules/vite/bin/vite.js --port 5176 --host 0.0.0.0

# 构建生产版本（部署前必做）
"$NODE" node_modules/vite/bin/vite.js build
```

> ⚠️ 构建前需先清掉旧 `dist/`（环境限制导致 safe-delete 报错，用 PowerShell `Remove-Item -Recurse -Force` 强删后再 build）。

---

## 七、部署全流程（Cloudflare Pages 真托管）

### 前置条件
- 域名已在**腾讯云**购买：`mingcheng-ai.com`（已实名通过）
- 注册免费 Cloudflare 账号

### 步骤 1 · 添加域到 Cloudflare
- 登录 dash.cloudflare.com → **Add a domain**（新版叫 "Add a domain"，在首页中间卡片或右上角 "Onboard a domain"）
- 输入 `mingcheng-ai.com` → 选 **Free** 计划
- 拿到两个 NS 地址：
  - `desiree.ns.cloudflare.com`
  - `lamar.ns.cloudflare.com`

### 步骤 2 · 腾讯云改 NS
- 腾讯云域名控制台 → `mingcheng-ai.com` → DNS 服务器 → 改为上面两个 Cloudflare NS → 保存
- 生效几分钟~几小时，Cloudflare 显示 "Your domain is now protected by Cloudflare" 即成功

### 步骤 3 · 部署（方案 B：AI 代部署）
- 准备 Cloudflare **API Token**（权限：Account → Cloudflare Pages → Edit）+ **Account ID**
- Account ID：`8171fb3f4e5ab10564cebde9fe1ca47c`
- 用 wrangler 部署（node workspace 已装 wrangler）：
  ```bash
  cd C:/Users/jx/.workbuddy/binaries/node/workspace
  CLOUDFLARE_API_TOKEN="<你的token>" \
  CLOUDFLARE_ACCOUNT_ID="8171fb3f4e5ab10564cebde9fe1ca47c" \
  C:/Users/jx/.workbuddy/binaries/node/versions/22.22.2/node.exe \
    ./node_modules/wrangler/bin/wrangler.js pages project create mingcheng-ai --production-branch main

  CLOUDFLARE_API_TOKEN="<你的token>" \
  CLOUDFLARE_ACCOUNT_ID="8171fb3f4e5ab10564cebde9fe1ca47c" \
  C:/Users/jx/.workbuddy/binaries/node/versions/22.22.2/node.exe \
    ./node_modules/wrangler/bin/wrangler.js pages deploy "C:/Users/jx/WorkBuddy/2026-08-10-16-29-14/mingcheng-website/dist" --project-name mingcheng-ai --branch main
  ```

### 步骤 4 · 绑定自定义域
- wrangler 4.x 已移除 `pages domain add` 子命令，改用 Cloudflare API：
  ```bash
  # POST https://api.cloudflare.com/client/v4/accounts/<AccountID>/pages/projects/mingcheng-ai/domains
  # body: { "name": "mingcheng-ai.com" }
  # Header: Authorization: Bearer <token>
  ```
- 绑域后状态 `initializing`（Cloudflare 自动签发 SSL + 加 DNS 记录），几分钟~1 小时生效

> 🔐 **安全提醒**：API Token 用完后去 Cloudflare → My Profile → API Tokens → Delete 撤销，已上线站点不受影响。

### 步骤 5 · 切换到 Git 自动部署（方案①，当前主力方式）

> 背景：原方案 B（wrangler 直传）每次部署都要 API Token，麻烦且不安全。已改为 **GitHub 连 Cloudflare 自动部署**，以后 `git push` 即上线，无需任何 token。

**前置**
- GitHub 仓库：`https://github.com/CaelumACE/mingcheng-website.git`（账号 `CaelumACE`）
- 本地仓库已 `git init`，分支 `master`，远程 `origin` 已设好
- 项目已纳入版本管理（43 文件，排除 node_modules / dist / v3-backup / v3.1-backup / mingcheng-dist.zip）

**Cloudflare 控制台连接步骤**
1. Cloudflare → **Workers & Pages** → 项目 `mingcheng-ai` → **Settings** → **Builds & deployments**
2. **Source** 区点 **Connect to Git** → 安装 Cloudflare Pages GitHub App → 选仓库 `CaelumACE/mingcheng-website`
3. 构建设置（照抄）：
   - **Production branch**：`master`（⚠️ 不是默认 `main`）
   - **Build command**：`npm run build`
   - **Build output directory**：`dist`
   - **Root directory**：留空（package.json 在仓库根）
   - **Automatic deployments**：Enabled
   - **Build system version**：Version 3
4. ⚠️ **必设环境变量（不然后直接构建失败）**：Settings → **Variables and Secrets** → **Production** → Add variable → `NODE_VERSION` = `22`（Vite 8 要求 Node ≥ 20.19，CF 默认不够）
5. 保存后触发首次构建，几秒~1 分钟 Success

**日常迭代流程（以后就这三行）**
```bash
cd mingcheng-website
git add .
git commit -m "改了什么"
git push        # Cloudflare 自动拉取 → npm install → build → 部署
```

---

## 八、当前访问地址

| 用途 | 地址 |
|------|------|
| **正式域名** | **https://mingcheng-ai.com** |
| Pages 原生子域（自动部署用，始终最新） | https://mingcheng-ai.pages.dev |
| 旧 CloudStudio 链接（可能失效） | https://cfdf45a0307d45ac85d154ed8e3547af.sh3.agentos-app.net |
| 本地预览 | http://localhost:5176 |
| GitHub 仓库 | https://github.com/CaelumACE/mingcheng-website.git |

> 💡 验证自动部署是否生效，优先看 `mingcheng-ai.pages.dev`（更新即时）；自定义域 `mingcheng-ai.com` 走边缘缓存，可能延迟几分钟~十几分钟。

---

## 九、后续迭代清单

- [ ] 合作伙伴 Logo 加白底圆角衬板（统一横版/方版视觉，更正式）
- [ ] BMW Logo 换高清版（现图 10KB，放大发虚）
- [ ] 补齐公司地址（联系页）
- [ ] 真实标杆案例客户名 / 更多案例图
- [ ] drone-training-scenes.png 是否加入无人机页
- [ ] 文案细节微调

> 迭代流程（Git 自动部署）：本地改 `src/` → `git add . && git commit -m "..." && git push` → Cloudflare 自动构建部署，无需 token。域名同步延迟较大时用 `mingcheng-ai.pages.dev` 实时验证。

---

## 十、踩坑记录

1. **Home.jsx 白屏**：合作伙伴区编辑时少一个 `</div>` 闭合标签，整页 JSX 错位编译失败 → 补回闭合标签，HMR 热更新恢复。
2. **dist/ 删除被拦**：环境 safe-delete 限制，统一用 PowerShell `Remove-Item -Recurse -Force` 强删后重建。
3. **v3 恢复不彻底**：Vite HMR 缓存旧样式 → `kill` 旧端口进程、重启 dev server 解决。
4. **端口顺延**：5175 被旧进程占，新版自动顺延到 **5176**，预览用 5176。
5. **Cloudflare 界面改名**：旧文档说 "Add a Site"，新版叫 "Add a domain" / "Onboard a domain"，位置在首页中间卡片。
6. **wrangler 4.x 移除 `pages domain add`**：绑域改用 Cloudflare REST API。
7. **绑域 pending→active**：CF Pages 绑自定义域后状态先 `initializing` 再 `pending`，属 SSL 签发排队，约 15–30 分钟自动转 `active`，无需手动干预（删除重绑可强制重新签发）。
8. **DNS 解析 NXDOMAIN**：绑域 active 后仍打不开，多为 NS 未真正生效或本地 DNS 缓存。用 `nslookup mingcheng-ai.com` 验证——能返回 `2606:4700:` 开头的 Cloudflare IPv6 即说明 NS 已生效。
9. **电脑打不开但手机能开（IPv6 路由问题）**：CF 默认开启 IPv6 兼容，会下发 AAAA 记录；部分家庭宽带 IPv6 路由不通，浏览器死磕 IPv6 连不上。解法：Cloudflare → 站点 → **Network → IPv6 Compatibility 关掉（Off）**，强制走 IPv4，对全站访客更稳。
10. **Vite 8 须 Node ≥ 20.19，CF 默认构建 Node 不够**：连 Git 时务必在 Variables and Secrets（Production）加 `NODE_VERSION=22`，否则 `npm run build` 直接报 Node 版本错误、部署失败。
11. **自定义域不更新 ≠ 传播延迟（最高频坑，优先排查）**：`git push` 后 `mingcheng-ai.pages.dev` 即时更新，但 `mingcheng-ai.com` 几小时都不动——根因是**自定义域 CNAME 钉死在旧部署 ID**，不是 DNS 传播。实测：最早 wrangler 直传时 CNAME 指向 `74eab2a1.mingcheng-ai.pages.dev`，切 Git 自动部署后规范子域变成 `mingcheng-ai.pages.dev`，但自定义域 CNAME 没自动改 → 永远返回旧部署。排查法：对比两域名的 `index.html`（JS/CSS 哈希相同但 HTML 不同即可确认）。**修复**：Cloudflare → Workers & Pages → 项目 → Custom domains → 若提示 CNAME 要从旧 `xxx.mingcheng-ai.pages.dev` 改成 `mingcheng-ai.pages.dev`，直接点 **Activate domain**（秒级生效，无需删域重加）。
12. **Deployments 列表全 Success 但域名没变**：每条部署其实都成功了（铁证），早期误判是"Production 不切换"或"传播延迟"——**真正根因见 #11（自定义域 CNAME 钉死旧部署）**。先按 #11 排查 CNAME，别在 Production/Promote 上白费功夫。
13. **GitHub PAT 用完即撤**：首次 push / 自动部署测试用的 classic PAT（ghp_ 开头）属临时凭证，测完立刻去 GitHub → Settings → Developer settings → Personal access tokens → 删除；站点运行与后续自动部署不受影响。
14. **SSH key 永久免 token 配置坑（2026-08-12 实战）**：
   - Windows OpenSSH `ssh-keygen -t ed25519 -N ""` 有时**失效导致 key 带密码**，非交互 `git push` 会卡在 `Enter passphrase`。验证法：`ssh-keygen -y -f 私钥` 若提示 passphrase 即带密码，需重生成（重生成后 `ssh-keygen -y` 应直接打印公钥）。
   - 路径坑：Git Bash 内置 ssh 认 `/c/Users/jx/.ssh`（HOME=`/c/Users/jx`），而 Windows 原生 `OpenSSH/ssh.exe` 只认 `C:\Users\jx\.ssh` 格式，混用会报 `Identity file not accessible`。**统一用 Git Bash 内置 ssh**（git push 默认即用它），不要手动调 Windows 版 ssh.exe 传 `/c/` 路径。
   - 配置：`git remote set-url origin git@github.com:CaelumACE/mingcheng-website.git`；公钥加到 GitHub → Settings → SSH and GPG keys → New SSH key。
   - **换 key 后必须同步更新 GitHub 公钥**：旧公钥对应已删私钥会变孤儿，GitHub 端不认新私钥 → `Permission denied (publickey)`，需去 GitHub 删除旧条、新增新公钥。
   - 验证连通：`ssh -T git@github.com`（Git Bash 内置）返回 `Hi CaelumACE!` 即成功，之后 `git push` 永久免 token。

---

## 十一、自动部署验证记录（2026-08-12 实测）

| 提交 | 说明 | 结果 |
|------|------|------|
| `d22f7ac` | 初始版本（wrangler 直传时代） | ✅ |
| `d59a86` / `810c69f` / `b059810` | 自动部署测试 v1/v2/v3（标题+隐藏注释标记） | ✅ 全部 Success，专属 URL 含标记 |
| `c6c9f10` | revert 测试标记 | ✅ |
| `e8458aa` | 清理测试标记，恢复干净版本 | ✅ 成为 Production |
| `834d11f` | **可见横幅测试**（蓝条横幅 + 标题加"· 部署测试"） | ✅ pages.dev 即时生效，自定义域延迟同步 |

**验证结论**：Git → Cloudflare 自动部署管线 100% 跑通。铁证 = pages.dev 子域在 push 后即时显示测试横幅与修改标题；Deployments 列表全部 Success。

### SSH 免 token 配置（2026-08-12 收尾）

之前每次 push 都要临时发 GitHub PAT，浩哥要求"永久保留、不想每次发 token"。改用 **SSH key 永久认证**：

- 本地生成无密码 ed25519 密钥：`C:\Users\jx\.ssh\id_ed25519`（公钥 `ssh-ed25519 ... mingcheng-website-deploy@local`）
- 已添加到 GitHub 账户：Settings → SSH and GPG keys
- git remote 已从 HTTPS 改为 SSH：`git@github.com:CaelumACE/mingcheng-website.git`
- 验证：`ssh -T git@github.com` 返回 `Hi CaelumACE! You've successfully authenticated`
- **以后 `git push` 永久免 token / PAT**

> ⚠️ 踩坑：Windows OpenSSH 用 `-N ""` 生成 key 时可能仍带密码（非交互 push 卡 passphrase），需重生成并确认 `ssh-keygen -y` 不提示密码；Git Bash 内置 ssh 认 `/c/Users/jx/.ssh`，Windows 原生 ssh.exe 认 `C:\Users\jx\.ssh`，混用报 "Identity file not accessible"，统一用 Git Bash 内置 ssh。

### 最终端到端测试（React 源码改动，2026-08-12 收尾）

为补上"只测过 index.html 外壳、从没验证 React 源码改动能否构建部署"的盲区，做了一次最终测试：

| 提交 | 说明 | 结果 |
|------|------|------|
| `1dec26b` | **改 React 源码** Home.jsx Hero 副标题加"· 终测OK"（之前测试只改 HTML 外壳） | ✅ 构建成功，JS bundle 含该字符串 |
| `f0d2e79` | revert 终测标记，恢复正式版 | ✅ 线上 JS 回到 `index-DJlropmm.js`（初始正式版哈希）、终测OK=0 |

**验证方法**：React SPA 内容由 JS 运行时渲染，curl 抓初始 HTML 看不到文案。正确做法是抓构建后的 JS bundle（`/assets/index-*.js`）搜源码字符串——`1dec26b` 部署后 bundle 含"铭澄互动 —— 让智能触手可及 · 终测OK"（计数=1），`f0d2e79` 回退后该字符串消失（计数=0）。

**最终结论**：从「改 React 源码 → `git push`（SSH 免 token）→ Cloudflare 自动 `npm run build` 编译 React → 部署上线」全链路 100% 闭环，可交付。日常改网站即 `git add . && git commit -m "..." && git push`。

---

*本笔记随项目迭代更新。*
