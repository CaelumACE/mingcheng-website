# 铭澄互动官网项目复盘

> 复盘时间：2026-08-12
> 项目周期：约前一晚 22:00 → 次日上午 11:30（高强度迭代）
> 参与：浩哥（决策/素材/后台操作）+ AI（开发/部署/排障/文档）
> 一句话结论：**企业官网从 0 到上线 + Git 自动部署（SSH 免 token）全链路闭环交付，过程踩 3 个关键坑全部定位修复并沉淀。**

---

## 一、项目概览

| 项 | 内容 |
|----|------|
| **目标** | 企业官网 + 自定义短域名 + 改一次即上线（免每次部署凭证） |
| **技术栈** | Vite 8 + React + react-router + TailwindCSS v3（纯静态 SPA） |
| **托管** | Cloudflare Pages（免备案、免费 SSL、真托管），非腾讯云（需备案） |
| **域名** | `mingcheng-ai.com`（腾讯云购买、实名、NS 改 Cloudflare） |
| **代码仓** | GitHub `CaelumACE/mingcheng-website`（分支 master，SSH 认证） |
| **最终形态** | `git push` → Cloudflare 自动 `npm run build` → 两域名同步 |

---

## 二、关键里程碑（时间线）

1. **素材集成期**：VR/AR 数字展厅图、无人机训练两张图、党建标杆案例图、6 家合作伙伴 Logo（银联/国网/北工大/BMW/IBM/凯捷）。风格定型为 v3 浅蓝玻璃风，Slogan「铭刻创新，澄澈未来」+「铭澄互动——让智能触手可及」。
2. **买域名**：腾讯云购 `mingcheng-ai.com`（更短的 MZ.com 等已被注册，选语义清晰的 AI 后缀）。
3. **首次上线**：Cloudflare 加域 → 改 NS → wrangler/API 直传部署 → 绑自定义域 → SSL 生效。手机先能开、电脑延时后通。
4. **Git 自动部署**：GitHub 建仓 → 本地 git init 推送 → Cloudflare 连 Git（分支 master / build=npm run build / 输出 dist / **NODE_VERSION=22**）。
5. **多轮真机测试**：HTML 外壳测试 → 可见横幅测试 → React 源码端到端测试，全部 Success。
6. **自定义域修复**：发现 CNAME 钉死旧部署，Activate domain 秒修。
7. **SSH 免 token**：生成无密码 ed25519 密钥，GitHub 加公钥，远程改 SSH，永久免 PAT。
8. **正式交付**：回退所有测试标记，两域名恢复正式版，仓库干净。

---

## 三、三个核心决策（浩哥拍板）

| 决策 | 选项 | 结论 | 理由 |
|------|------|------|------|
| **域名** | 更短但被注册的（MZ.com）/ `mingcheng-ai.tech` / `mingcheng-ai.com` | **mingcheng-ai.com** | 语义清晰、未被抢注、AI 后缀契合公司定位，不强行追短 |
| **托管商** | 腾讯云（需备案）/ Cloudflare（免备案） | **Cloudflare** | 个人/企业站免 ICP 备案即可公网访问，免费 SSL |
| **部署方式** | 每次发 token / 控制台上传 / Git 自动部署 | **Git 自动部署 + SSH 免 token** | 改一次 push 即上线，终态自闭环，不依赖 AI 每次介入 |

---

## 四、三个关键坑（已沉淀为铁律/笔记）

### 坑 1 · 自定义域不更新 ≠ 传播延迟（最致命，耗时最长）
- **现象**：`git push` 后 `mingcheng-ai.pages.dev` 即时更新，但 `mingcheng-ai.com` 6 小时不变。
- **根因**：最早 wrangler 直传时，自定义域 CNAME 钉在带部署 ID 的旧子域 `74eab2a1.mingcheng-ai.pages.dev`；切 Git 后规范子域变 `mingcheng-ai.pages.dev`，CNAME 未自动改 → 永远返回旧部署。
- **修复**：Cloudflare → Custom domains → 点 **Activate domain**（CNAME 自动切到新子域），秒级生效。
- **教训**：已记入 `~/.workbuddy/MEMORY.md`「Cloudflare Pages 部署铁律，优先排查」。

### 坑 2 · Vite 8 需要 Node ≥ 20.19
- Cloudflare Pages 默认构建 Node 偏低会直接构建失败。
- **必设**：项目 Variables and Secrets（Production）加 `NODE_VERSION=22`。

### 坑 3 · Windows 下 SSH key 带密码 + 路径混用
- `ssh-keygen -N ""` 在 Windows OpenSSH 下未生效，第一把 key 实际带密码 → 非交互 `git push` 卡 passphrase。
- 重生成无密码 key；Git Bash 内置 ssh 认 `/c/Users/jx/.ssh`，Windows 原生 ssh.exe 只认 `C:\Users\jx\.ssh`，混用报 "Identity file not accessible"。统一用 Git Bash 内置 ssh。

---

## 五、协作风格复盘（最核心，供未来复用）

### 浩哥的沟通特征
1. **指令极简、碎片化、常带截图**：几乎每条都很短（"做了吗""先出一版""第五步怎么设置"），大量 @截图要求"看下/定位"。不写长篇需求文档。
2. **先要结果再迭代**：典型「先出一版效果」→ 看实际 → 提调整，不先讨论方案细节。快速出可看版本优先于完美。
3. **决策给选项、不啰嗦**：喜欢 AI 列 2–4 个选项 + 关键权衡，他选（"走方案1"）。避免大段背景铺垫。
4. **要明确的下一步清单**：多次问"还要做什么吗""详细说下第 1 步"。每个阶段需要 actionable checklist，并标明「需你后台操作 / 我替不了」。
5. **后台操作他来做、按指引走**：Cloudflare / GitHub 等需账号的动作他配合、发截图确认；他不深究原理，只要"照做即可"。步骤要图文化、可复制（命令/token 直接给）。
6. **信任 AI 审美与判断**：给素材时说"整体协调、别硬塞、你斟酌放哪"——合理决策即可，不必事事请示，事后展示。
7. **强经验沉淀意识**：明确"记住这个问题优先排查""做成笔记""更新项目文档"。踩坑即时进笔记 + 长期记忆。
8. **希望主动同步进度**：多次"进行到哪一步了""还是没同步"——卡点/等待时主动报告状态，不让他猜。
9. **安全靠提醒**：PAT 用完即撤、确认不落盘；有意识但依赖 AI 提醒。
10. **终态要自闭环**：最终要"永久免 token"、自己能改能部署，不依赖每次介入。

### AI 协作中做对的
- 复杂操作翻译成「照做 X 步」清单；卡点用 curl/截图给铁证而非猜测。
- 踩坑立刻记进项目笔记 + 长期记忆，并标注"优先排查"。
- 主动同步进度（"进行到哪一步了"）。
- 交付物让他能独立运维（SSH、push 命令、笔记）。

### 可改进
- 早期曾误判"域名没更新=部署没通"，多绕了一轮——应**第一时间对比自定义域 vs pages.dev 的响应**再下结论（现已固化为铁律）。
- 测试验证一度依赖轮询、被工具输出吞掉，应更早用「专属部署 URL + JS bundle 搜字符串」的硬验证法。

---

## 六、可复用的项目交付范式（未来做别的项目的套路）

1. **素材先行、快速出效果**：先集成能看的版本，再迭代，不追求本地完美再上线。
2. **上线即验证、真机测试**：上线后发截图/抓 HTML 核对，必要时做"可逆小改动"端到端验证。
3. **经验即时沉淀**：每个阶段产出/更新项目笔记；跨项目技术坑进长期记忆，标注"优先排查"。
4. **终态自闭环**：交付时把"以后怎么自己改/部署"说清（命令 + 免凭证方案），不依赖 AI 反复介入。
5. **安全收尾**：用过的临时凭证（PAT/Token）主动提示撤销，确认不落盘。

---

## 七、未来项目启动清单（套用检查项）

- [ ] 确认目标、技术栈、是否需要域名/备案
- [ ] 素材清单（图/文/Logo）是否齐，风格基调定调
- [ ] 托管与域名方案（备案 vs 免备案）
- [ ] 部署方式（自动部署 + 免凭证优先）
- [ ] 上线后真机验证（自定义域 vs 子域对比）
- [ ] 项目笔记 + 踩坑沉淀
- [ ] 临时凭证撤销、交付自闭环说明

---

*本复盘与「项目笔记.md」互补：笔记偏操作手册，复盘偏方法论与协作风格。*
